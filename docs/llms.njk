---
permalink: llms.txt
eleventyExcludeFromCollections: true
---
{%- set nav = collections.all | navigation -%}
# {{ metadata.title }}

> {{ metadata.description | safe }}

Feo.css provides structure (a reset, bare-element defaults, layout primitives, and utilities) in cascade layers, and leaves tokens, colors, and components to your project. Install with `npm install feo-css@beta`, or load it from a CDN: `<link rel="stylesheet" href="https://unpkg.com/feo-css@beta/feo.min.css">`.

## Start here

Read these before writing any CSS with Feo.css. The full documentation is also available as a single file: [llms-full.txt]({{ metadata.url }}/llms-full.txt).

{% for key, item in nav %}{% if key in ["principles", "layout"] %}- [{{ item.title | safe }}]({{ metadata.url }}{{ item.md }}): {{ item.description | safe }}
{% endif %}{% endfor %}
## Docs

{% for key, item in nav %}{% if not item.sub.length and key != "principles" %}- [{{ item.title | safe }}]({{ metadata.url }}{{ item.md }}){% if item.description %}: {{ item.description | safe }}{% endif %}
{% endif %}{% endfor %}
{%- for key, item in nav %}{% if item.sub.length %}
## {{ item.title | safe }}

- [{{ item.title | safe }}]({{ metadata.url }}{{ item.md }}): overview
{% for sub in item.sub %}- [{{ sub.title | safe }}]({{ metadata.url }}{{ sub.md }})
{% endfor %}{% endif %}{% endfor %}
